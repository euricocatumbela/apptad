import React from "react";
import styled from "styled-components";

const CreateFile = () => {
  return (
    <Wrapper>
      <WrapperContent>
        <p>Please select a content to upload</p>
        <input type="title"></input>
      </WrapperContent>
    </Wrapper>
  );
};

export default Calendar;

const Wrapper = styled.div``;
const WrapperContent = styled.div``;
